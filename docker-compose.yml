version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: app-store-db
    environment:
      POSTGRES_USER: appstore
      POSTGRES_PASSWORD: appstore_password
      POSTGRES_DB: appstore_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appstore"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: app-store-cache
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: app-store-api
    environment:
      NODE_ENV: development
      PORT: 5000
      DATABASE_URL: postgresql://appstore:appstore_password@postgres:5432/appstore_db
      REDIS_URL: redis://redis:6379
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./src:/app/src
      - ./dist:/app/dist
    command: npm run start:dev

volumes:
  postgres_data:
  redis_data:
